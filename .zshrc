HISTFILE=~/.histfile
HISTSIZE=10000
SAVEHIST=10000
setopt autocd
bindkey -v

# some aliases
alias mkdir="mkdir -p"

# git aliases
alias ginit="git init"
alias gadd="git add -A"
alias gcm="git commit -m"
alias gstat="git status"
